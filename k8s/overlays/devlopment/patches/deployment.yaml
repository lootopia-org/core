apiVersion: apps/v1
kind: Deployment
metadata:
  name: core
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: core
          volumeMounts:
            - name: env-file
              mountPath: /app/.env
              subPath: .env-dev
          env:
            - name: ENV
              value: "development"

